fastapi==0.116.0
uvicorn[standard]==0.35.0
pydantic==2.10.6
python-dotenv==1.0.1
requests==2.32.3
torch==2.6.0
transformers==5.17.0
# GGUF 读取（#28：官方 QAT 权重 google/gemma-4-E2B-it-qat-q4_0-gguf 转 .tqwen）。
# 只用它的 quants.dequantize 与 constants，不引入 llama.cpp。
# 关键价值：提供 Q4_0 / Q6_K 的 numpy 参考实现，避免自己按记忆实现 block_q6_K
# （256 值/超块、6-bit scale 打包，写错是静默乱码）。
gguf==0.19.0
accelerate==1.5.1
# bitsandbytes 0.42.0 无 Windows wheel，会让 Windows 原生 pip install 直接失败。
# 代码中仅 edge_runtime._load_model_with_quantization 懒加载（bnb 量化路径，
# 默认不启用），跳过安装不影响端侧 CPU 推理与索引构建。
# Windows 上确需 bnb 量化时：pip install "bitsandbytes>=0.44"（官方已发 Windows wheel）。
bitsandbytes==0.42.0; sys_platform != "win32"
modelscope==1.20.0
faiss-cpu==1.10.0
numpy==2.3.2
# 中文分词（text_utils.tokenize 的 M0 升级）：长查询不再退化成单个整 token。
# 可选依赖：缺失时 tokenize 自动降级为纯规则口径，业务不中断。纯 Python，无原生依赖。
jieba==0.42.1
